package com.bochat.app.app;

import android.content.Intent;

import com.alibaba.android.arouter.launcher.ARouter;
import com.bochat.app.model.bean.UserEntity;

/**
 * Author      : FJ
 * CreateDate  : 2019/05/30 13:51
 * Description :
 */

public class Router {
    
    private void test(){
        ARouter.getInstance().build(RouteTable.BOOK_ADDRESS_FRIEND_DETAIL)
                .withString(RouteExtra.ConversationId_String, "")
                .withBoolean(RouteExtra.IsSearchFriend_Boolean, true)
                .navigation();
    }
    
    public class FriendDetailRouter{
        private String id;
        private boolean isLocal;
        public FriendDetailRouter(String id, boolean isLocal) {
            this.id = id;
            this.isLocal = isLocal;
        }
    }
    
    private void onViewRefresh(){
        Intent intent = new Intent();
        UserEntity userEntity = create(UserEntity.class);
        
        
        
    }

    public <T> T create(final Class<T> service) {
        
        
        
        return null;
    }
    
}
